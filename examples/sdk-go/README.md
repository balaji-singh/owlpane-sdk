# Go example (published module)

Uses `go get github.com/balaji-singh/owlpane-sdk/packages/go@v0.1.1` (tag `packages/go/v0.1.1`).

```bash
cp ../.env.console.example ../.env
./run-console.sh
```

Local monorepo module:

```bash
OWLPANE_SDK_SOURCE=local ./run-console.sh
```
