# Python example (`owlpane` on PyPI)

```bash
cp ../.env.console.example ../.env
./run-console.sh
```

Until PyPI is published, use the monorepo package:

```bash
OWLPANE_SDK_SOURCE=local ./run-console.sh
```
